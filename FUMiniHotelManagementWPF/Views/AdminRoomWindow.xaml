<Window x:Class="FUMiniHotelManagementWPF.Views.AdminRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FUMiniHotelManagementWPF.Views"
        mc:Ignorable="d"
        Title="Quản lý phòng" Height="500" Width="900" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="RoomDataGrid" ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom, Mode=TwoWay}"
              AutoGenerateColumns="False" Grid.Row="0" Margin="0,0,0,10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomId}" Width="60"/>
                <DataGridTextColumn Header="Số phòng" Binding="{Binding RoomNumber}" Width="120"/>
                <DataGridTextColumn Header="Loại phòng" Binding="{Binding RoomType.RoomTypeName}" Width="120"/>
                <DataGridTextColumn Header="Mô tả loại" Binding="{Binding RoomType.TypeDescription}" Width="150"/>
                <DataGridTextColumn Header="Ghi chú loại" Binding="{Binding RoomType.TypeNote}" Width="120"/>
                <DataGridTextColumn Header="Mô tả phòng" Binding="{Binding RoomDetailDescription}" Width="150"/>
                <DataGridTextColumn Header="Sức chứa tối đa" Binding="{Binding RoomMaxCapacity}" Width="100"/>
                <DataGridTextColumn Header="Giá/ngày" Binding="{Binding RoomPricePerDay}" Width="100"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding RoomStatus}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Tìm" Command="{Binding SearchCommand}" Width="80"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" >
            <Button Content="Thêm" Command="{Binding AddCommand}" Width="80" Margin="5"/>
            <Button Content="Sửa" Command="{Binding EditCommand}" Width="80" Margin="5"/>
            <Button Content="Xóa" Command="{Binding DeleteCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
