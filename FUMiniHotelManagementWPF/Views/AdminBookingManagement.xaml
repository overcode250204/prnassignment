<Window x:Class="FUMiniHotelManagementWPF.Views.AdminBookingManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FUMiniHotelManagementWPF.Views"
        mc:Ignorable="d"
        Title="AdminBookingManagement" Height="650" Width="1100" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Danh sách đặt phòng" Grid.Row="0" Margin="0,0,0,10">
            <DataGrid x:Name="BookingDataGrid" ItemsSource="{Binding Reservations}" SelectedItem="{Binding SelectedReservation, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã đặt phòng" Binding="{Binding BookingReservationId}" Width="100"/>
                    <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerId}" Width="100"/>
                    <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalPrice}" Width="100"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding BookingStatus}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <GroupBox Header="Chi tiết đặt phòng" Grid.Row="1" Margin="0,0,0,10">
            <DataGrid x:Name="DetailDataGrid" ItemsSource="{Binding SelectedReservation.BookingDetails}" SelectedItem="{Binding SelectedDetail, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Phòng" Binding="{Binding RoomId}" Width="100"/>
                    <DataGridTextColumn Header="Giá thực tế" Binding="{Binding ActualPrice}" Width="100"/>
                    <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" >
            <Button Content="Thêm đặt phòng" Command="{Binding AddReservationCommand}" Width="120" Margin="5"/>
            <Button Content="Sửa đặt phòng" Command="{Binding EditReservationCommand}" Width="120" Margin="5"/>
            <Button Content="Xóa đặt phòng" Command="{Binding DeleteReservationCommand}" Width="120" Margin="5"/>
            <Button Content="Thêm chi tiết" Command="{Binding AddDetailCommand}" Width="120" Margin="5"/>
            <Button Content="Sửa chi tiết" Command="{Binding EditDetailCommand}" Width="120" Margin="5"/>
            <Button Content="Xóa chi tiết" Command="{Binding DeleteDetailCommand}" Width="120" Margin="5"/>
            <Button Content="Reload" Command="{Binding ReloadCommand}" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
